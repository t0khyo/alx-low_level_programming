#include "main.h"
/**
 * _islower - return 1 or 0
 * @c: character to test.
 * this program is compiled on.
 * Return: 0 if anything rather than chracter.
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
